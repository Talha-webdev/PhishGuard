# Dataset

The PhishGuard model was trained using phishing/spam email data, including the CEAS dataset used during development.

The complete dataset is intentionally not committed to this repository. This keeps the repository smaller and avoids redistributing dataset files unnecessarily.

To reproduce training, obtain the dataset from its original legitimate source and place the required files in this folder using the filenames expected by the notebook.
